using Plots, Printf, LinearAlgebra, DelimitedFiles

function Derivatives_calc(M, t_sc, t_net, n)
    #This function computes all the partial derivatives needed for advance ratio
        #M is mach number
        #t_sc is the settling chamber temperature
        #t_net is the net thrust 
        #n is the revolution speed in rpm
end